# VectorCraft-Py 使用文档

## 1. 软件简介

VectorCraft-Py 是一个基于 Python 的矢量图形编辑系统，适用于绘制流程图、组织结构图、电路图和基础几何图形。

本软件使用 Tkinter 构建桌面界面，使用 Pillow 作为图像缓冲区，并在程序内部实现了部分计算机图形学算法，例如 Bresenham 直线绘制、多边形扫描线填充、椭圆绘制、贝塞尔曲线采样、几何变换和 Cohen-Sutherland 线段裁剪。

## 2. 运行环境

推荐环境：

```text
Python 3.10 或更高版本
Pillow 12.0.0 或更高版本
Windows / macOS / Linux
```

安装依赖：

```bash
pip install -r requirements.txt
```

启动程序：

```bash
python main.py
```

如果已经安装 Pillow，也可以直接运行：

```bash
python main.py
```

## 3. 界面组成

程序界面主要分为五个区域：

| 区域 | 功能 |
| --- | --- |
| 顶部工具栏 | 新建、打开、保存、导出、撤销、重做、复制、粘贴、删除、镜像、裁剪、图层操作 |
| 模板与视图栏 | 载入流程图、组织结构图、电路图模板，开启网格和吸附，调整缩放 |
| 左侧工具箱 | 选择工具、平移工具、连接线工具、文本工具、画笔工具、基础图形、三维图元、流程图图元、电路图图元 |
| 中央画布 | 绘制、选择、移动和编辑图形 |
| 右侧属性面板 | 精确修改图形坐标、大小、角度、线宽、颜色、线型、文本和连接线样式 |
| 底部状态栏 | 显示当前状态、鼠标坐标和缩放比例 |

## 4. 基础绘图

### 4.1 绘制普通图形

1. 在左侧工具箱选择需要的图元，例如“矩形”“椭圆”“菱形”“三角形”。
2. 在画布中按住鼠标左键拖拽。
3. 松开鼠标后，图形会被创建并自动选中。

支持的基础图形包括：

- 矩形
- 圆角矩形
- 椭圆
- 菱形
- 三角形
- 直线
- 箭头
- 文本框

### 4.2 绘制文本

1. 选择左侧“文本”工具。
2. 在画布中拖拽生成文本框。
3. 双击文本框，输入或修改文本内容。
4. 也可以在右侧属性面板的“文本”输入框中修改文本。

### 4.3 使用画笔标注

1. 选择左侧“画笔”工具。
2. 在画布中按住鼠标左键拖动，即可自由书写、圈画或标注。
3. 顶部第二行的“画笔”数值框可调整粗细，“画笔颜色”可调整颜色。
4. 画笔笔迹保存为可编辑图元，可以选中后移动、删除、改线宽或改颜色。

### 4.4 绘制流程图图元

左侧“流程”区域提供常用流程图图元：

- 开始 / 结束
- 处理
- 判断
- 输入 / 输出
- 数据库
- 文档

选择对应图元后，在画布中拖拽即可绘制。

### 4.5 绘制电路图图元

左侧“电路”区域提供基础电路符号：

- 电阻
- 电容
- 电池
- 开关
- 接地
- LED

这些图元可以配合连接线工具绘制简单电路图。

### 4.6 绘制三维几何图元

左侧“三维”区域提供适合讲解空间几何题的图元：

- 正方体
- 长方体
- 球体
- 三维坐标系

选择对应图元后，在画布中拖拽即可绘制。正方体和球体会自动保持等宽等高；长方体可以自由调整宽高；三维坐标系包含 x/y/z 轴、刻度和辅助网格线，便于标注空间点、线、面关系。

将正方体、长方体或球体放入三维坐标系范围内时，程序会自动显示顶点坐标或球心坐标，便于讲解空间几何题。

## 5. 选择与编辑

### 5.1 选择图形

1. 点击左侧“选择”工具。
2. 单击画布中的图形即可选中。
3. 在空白区域拖拽可以框选多个图形。
4. 按住 Shift 再点击图形，可以进行多选或取消选择。

### 5.2 移动图形

1. 使用“选择”工具选中一个或多个图形。
2. 按住鼠标左键拖动图形。
3. 松开鼠标后完成移动。

如果开启“吸附”，图形移动时会自动对齐到网格。

### 5.3 删除图形

选中图形后，可以通过以下方式删除：

- 点击顶部“删除”按钮
- 按键盘 `Delete`

### 5.4 复制和粘贴

选中图形后：

- 点击“复制”，再点击“粘贴”
- 或使用快捷键 `Ctrl+C` 和 `Ctrl+V`

粘贴后的图形会相对原图形偏移一段距离，并自动选中。

## 6. 图形属性修改

选中图形后，可以在右侧属性面板修改：

| 属性 | 说明 |
| --- | --- |
| X | 图形左上角横坐标 |
| Y | 图形左上角纵坐标 |
| 宽 | 图形宽度 |
| 高 | 图形高度 |
| 角度 | 图形旋转角度 |
| 线宽 | 描边线宽 |
| 文本 | 图形内部显示的文字 |
| 填充色 | 图形内部颜色 |
| 描边色 | 图形边框颜色 |
| 线型 | 实线、虚线、点线 |
| 连接 | 连接线样式，包括折线、直线、曲线 |

修改数值后按 `Enter`，或让输入框失去焦点，即可应用修改。

## 7. 几何变换

软件支持以下几何变换：

- 平移：直接拖动图形
- 缩放：在右侧属性面板修改宽度和高度
- 旋转：在右侧属性面板修改角度
- 水平镜像：点击顶部“水平镜像”
- 垂直镜像：点击顶部“垂直镜像”

这些变换会保存到自定义图形数据结构中，重新打开文件后仍然保留。

## 8. 连接线

### 8.1 创建连接线

1. 点击左侧“连接”工具。
2. 单击第一个图形作为起点。
3. 再单击第二个图形作为终点。
4. 系统会自动生成连接线。

### 8.2 连接线自动跟随

连接线记录的是起点图形和终点图形的 ID。  
当起点或终点图形移动后，连接线会自动重新计算位置并跟随图形。

### 8.3 连接线样式

选中连接线后，可以在右侧属性面板修改连接样式：

- `orthogonal`：折线连接
- `straight`：直线连接
- `curve`：曲线连接

## 9. 裁剪功能

软件支持 Cohen-Sutherland 线段裁剪。

使用方法：

1. 绘制一个普通图形作为裁剪区域，例如矩形。
2. 绘制一条或多条直线 / 箭头线。
3. 同时选中裁剪区域和线段。
4. 点击顶部“线段裁剪”按钮。
5. 线段会被裁剪到选中图形的包围区域内。

如果线段完全在裁剪区域外，则会被裁剪为空。

## 10. 网格与吸附

模板与视图栏中提供两个开关：

- 网格：显示或隐藏画布网格
- 吸附：绘制和移动时自动对齐网格

网格吸附可以帮助快速绘制整齐的流程图、组织结构图和电路图。

## 11. 对齐与排布

右侧属性面板提供多图形对齐功能：

- 左对齐
- 水平居中
- 顶对齐
- 垂直居中
- 横向分布
- 纵向分布

## 12. 模板功能

程序内置六个模板：

| 模板 | 用途 |
| --- | --- |
| 流程图模板 | 生成包含数据接收、完整性判断、异常分支、审批分支、数据库保存、报表导出的完整流程 |
| 组织结构图模板 | 生成包含项目负责人、五个职能小组、子职责节点和交付物说明的层级结构图 |
| 电路图模板 | 生成包含电源、开关、限流电阻、LED、电流表、接地、滤波电容和泄放电阻的电路示意图 |
| UML 类图模板 | 展示 Shape / Style / Document / Page / History / Renderer 等核心类的属性、方法及聚合 / 组合 / 依赖关系 |
| 甘特图模板 | 展示项目 9 项主任务、3 个里程碑、5 类工作分类的图例 |
| 时序图模板 | 展示「保存与导出」过程中 5 条生命线之间的同步调用、返回调用和自身循环 |

点击模板按钮后，当前画布会载入对应模板。

## 13. 文件保存与打开

### 13.1 保存工程

点击顶部“保存”按钮，选择保存路径。  
工程文件保存为自定义 JSON 格式，推荐扩展名：

```text
.vcraft.json
```

保存内容包括：

- 画布尺寸
- 背景色
- 网格尺寸
- 所有图形对象
- 图形类型
- 坐标、大小、旋转角度
- 填充色、描边色、线宽、线型
- 文本内容
- 连接线起点和终点关系

### 13.2 打开工程

点击顶部“打开”按钮，选择 `.vcraft.json` 或 `.json` 文件。  
打开后，图形会被重新显示，并可以继续编辑。

## 14. 导出功能

软件支持两种导出格式：

| 格式 | 用途 |
| --- | --- |
| PNG | 导出为位图图片，可放入实验报告或 PPT |
| SVG | 导出为矢量图，可继续用于矢量编辑或网页展示 |

导出方式：

- 点击顶部“PNG”按钮导出图片
- 点击顶部“SVG”按钮导出矢量文件

## 15. 撤销与重做

软件支持历史记录：

- 撤销：点击“撤销”或按 `Ctrl+Z`
- 重做：点击“重做”或按 `Ctrl+Y`

绘制、移动、删除、粘贴、属性修改、排布等操作都会进入历史记录。

## 16. 快捷键

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+S` | 保存 |
| `Ctrl+O` | 打开 |
| `Ctrl+Z` | 撤销 |
| `Ctrl+Y` | 重做 |
| `Ctrl+C` | 复制 |
| `Ctrl+V` | 粘贴 |
| `Delete` | 删除选中图形 |
| `Esc` | 取消当前操作并返回选择工具 |
| 鼠标滚轮 | 放大或缩小画布 |

## 16.5 新增功能速查

### 16.5.1 智能形状识别

打开顶部工具栏的「智能识别」复选框后，选择左侧「画笔」工具，在画布上手绘草图，松开鼠标时程序会自动识别为以下图元之一并替换原始笔迹：

- 闭合且四角靠近矩形角落 → 矩形
- 闭合且四角靠近边线中点 → 菱形
- 闭合且形状接近椭圆 → 椭圆 / 圆
- 闭合且只有 3 个顶点 → 三角形
- 开放且端点连线接近笔迹长度 → 直线

如果识别置信度不够，会保留原始画笔笔迹，不影响标注效果。

### 16.5.2 数学函数图像

1. 点击「函数图像」按钮。
2. 在编辑框中按行输入表达式，如 `sin(x)`、`x**2 - 3`、`exp(-x*x/2)`，可同时输入多条。
3. 设置 `x` 范围和采样数。
4. 选择「替换当前画布」或「生成到新页面」。

支持的函数包括 `sin / cos / tan / asin / acos / atan`、`sinh / cosh / tanh`、`exp / log / log10 / log2 / ln`、`sqrt / abs / floor / ceil / pow`，常量 `pi / e / tau`，幂运算可用 `^` 或 `**`。

### 16.5.3 思维导图

1. 点击「思维导图」按钮。
2. 输入大纲，每行一个节点，使用 2 个空格或一个 Tab 表示一级缩进。
3. 选择「替换当前画布」或「生成到新页面」。

布局算法：先递归测量每个子树的总高度，再依次为同层节点分配纵向区间，最后整体平移到正坐标空间。父子节点之间使用「曲线」连接器，会随节点移动自动跟随。

### 16.5.4 UML 类图 / 甘特图 / 时序图

点击「UML 类图」、「甘特图」、「时序图」按钮，程序会立即在当前页面载入对应的工程级模板。所有图元都是矢量化的可编辑节点和连接线，可继续移动、改色、改文字。

### 16.5.5 多页画布

- 点击「页面管理」打开页面面板，可新建、复制、重命名、删除、上移、下移页面。
- 顶部工具栏「新页面」、「上一页」、「下一页」可快速切换。
- 工程文件 `.vcraft.json` 单页时保持旧格式；多页时自动升级为工作区格式（包含 `kind: "workspace"` 字段），打开时会自动识别并兼容旧文件。
- 每个页面拥有独立的撤销/重做历史。

## 17. 建议演示流程

1. 启动程序：`python main.py`
2. 展示流程图模板。
3. 新增一个流程图节点。
4. 使用连接工具连接两个节点。
5. 移动节点，展示连接线自动跟随。
6. 修改节点颜色、线宽和文字。
7. 复制、粘贴、删除图形。
8. 使用对齐或等距分布功能。
9. 绘制一条线段并演示裁剪。
10. 保存为 `.vcraft.json`。
11. 重新打开保存文件，继续编辑。
12. 导出 PNG 或 SVG。

## 18. 常见问题

### 18.1 程序无法启动

请确认已经安装 Pillow：

```bash
pip install pillow
```

然后重新运行：

```bash
python main.py
```

### 18.2 中文显示异常

程序优先使用 Windows 系统字体：

```text
Microsoft YaHei
SimHei
Arial
```

如果系统缺少中文字体，可能导致中文显示不正常。建议安装常见中文字体后重新运行。

### 18.3 画布移动或绘制不够整齐

请打开“网格”和“吸附”开关。  
开启后，图形会自动对齐到网格点。

### 18.4 保存后还能继续编辑吗

可以。  
`.vcraft.json` 保存的是自定义矢量图形数据，不是普通图片。重新打开后，所有图形仍然可以移动、修改属性、复制、删除和导出。

## 19. 作业要求对应关系

| 作业要求 | 软件实现 |
| --- | --- |
| 选择、移动、复制、粘贴、删除 | 已实现 |
| 几何变换 | 支持平移、缩放、旋转、镜像 |
| 裁剪功能 | 支持 Cohen-Sutherland 线段裁剪 |
| 自定义图形数据结构 | 使用 `.vcraft.json` 保存工程，单页直接存 `Document`，多页则升级为 `workspace` 格式 |
| 重新显示保存图形 | 支持打开 JSON 工程文件，自动识别单页/多页 |
| 继续编辑已打开文件 | 支持 |
| 复杂图元 | 支持流程图、组织结构图、电路图、UML 类图、甘特图、时序图等多种图元 |
| 实用性 | 可绘制流程图、组织结构图、电路图、UML 类图、甘特图、时序图、思维导图、函数图像 |
| 创新设计 | 智能连接线、文本生成流程图、思维导图自动布局、函数图像绘制、智能形状识别、算法可视化演示、自由画笔标注、三维坐标自动标注、操作回放、多页画布、导出 PNG/SVG |
